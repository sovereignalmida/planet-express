# Test homelab stub of the live host's network stack .env (read by casa_leela.py's
# check_vpn_port_forwarding via GLUETUN_ENV_FILE). Fake values only; there is no gluetun
# or qBittorrent in the homelab, so that check reports its "unreachable" path.
GSP_GTN_API_KEY=homelab-fake-api-key-not-a-secret
